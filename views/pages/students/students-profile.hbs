<div class="container">

    {{#with theUser}}
    <h1>{{userName}}</h1>
    <hr>
    <h3>{{name}}</h3>
    <p>{{description}}</p>
    <p>{{profileImg}}</p>
    <a href="/students" class="btn btn-dark">Volver al listado</a>

    {{/with}}

    {{#if isBoss}}
    <a href="/students/editar?user_id={{id}}" class="btn btn-dark">EDIT STUDENT</a>
    <a href="#" class="btn btn-danger">DELETE STUDENT</a>
    {{/if}}


</div>