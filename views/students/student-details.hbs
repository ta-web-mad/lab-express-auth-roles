
{{#unless errorMessage}}
  <h1>{{student.username}}</h1>
  <h2>{{student.name}}</h2>
  <p>{{student.description}}</p>
  <img src={{student.profileImg}} alt="User Image">

{{#if isOwn}}
    <a href="/students/edit/{{student._id}}">Edit</a>
    <a href="/students/delete/{{student._id}}">Delete</a>
{{/if}}

{{#if isAdmin}}
    <a href="/students/edit/{{student._id}}">Edit</a>
    <a href="/students/delete/{{student._id}}">Delete</a>
{{/if}}

{{/unless}}

{{#if errorMessage}}
  <div style="margin-top: 20px; padding: 5px; color: white; background: red; text-align: center; font-size: .95em">
    {{errorMessage}}
  </div>
{{/if}}

{{!-- {{!-- {{#if isOwn}}
  <a href="/libros/editar/{{book.id}}">Editar</a>
{{/if}} -- --}}