<div class="container">

    <h1>{{username}}</h1>

    <hr>

    <div class="row">
        <div class="col-md-8">
            <p><strong>Imagen</strong>: {{user.profileImg}}</p>
            <p><strong>Nombre</strong>: {{user.name}}</p>
        </div>
        <div class="col-md-4">
            <p><strong>Description</strong>: {{user.description}}</p>
        </div>
    </div>

    <hr>

    <a href="/student/student-list" class="btn btn-dark">Volver al listado</a>
    {{#if isAdmin}}
    <br>
    <br>
    <a href="#" class="btn btn-danger">Eliminar</a>
    {{/if}}

</div>