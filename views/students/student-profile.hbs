<div class="container">


    <div class="row justify-content-between">
        <div class="col-md-4">
            <h1>{{student.username}}</h1>
        </div>
        <div class="col-md-4 ">
            <div class="btn-group">
                {{#if isPM}}

                <form action="/students/{{id}}/delete" method="POST">
                    <button type="submit" class="btn btn-danger btn-lg mr-2">Eliminar</button>
                </form>
                {{/if}}

                <br>
                <a class="btn btn-primary btn-lg" href="/students/{{student.id}}/edit">Editar</a>
            </div>
        </div>
    </div>


    <hr>

    <div class="row">
        <div class="col-md-8">
            <img src="{{student.profileImg}}" alt="{{name}}">
            <p><strong>Nombre</strong>: {{student.name}}</p>
        </div>
        <div class="col-md-4">
            <p><strong>Descripción</strong>: {{student.description}}</p>
            <p><strong>Rol</strong>: {{student.role}}</p>
        </div>
    </div>

    <hr>


    <a href="/students" class="btn btn-dark">Volver al listado</a>

</div>