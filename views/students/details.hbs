<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">

            <h1>Detalles de {{user.username}}</h1>
            <hr>
            <p>Email: {{user.email}}</p>
            <hr>
            <p>Descripción: {{user.description}}</p>
            <hr>
            <img src="{{user.profileImg}}" class="img-fluid" alt="Profile image">
            <br>
            <hr>
            <a href="/listado" class="btn btn-dark">Volver al listado</a>
            <hr>
            {{#if isPM}}
            <a href="/editar/{{user._id}}" class="btn btn-info">Editar</a>
            <hr>
            <form action="/eliminar/{{user._id}}" method="POST">
                <input type="submit" value="Eliminar" class="btn btn-danger">
            </form>
            {{/if}}
            <br><br>

        </div>
    </div>
</div>