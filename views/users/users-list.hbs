<h1>Students List</h1>
<hr>
{{#each studentList}}
{{!-- //quiero que saques esto --}}
<li class="list-group-item d-flex justify-content-between align-items-start">
    <div class="ms-2 me-auto">
        <div class="fw-bold">{{username}}</div>
        <a href="/students/{{_id}}" class="btn btn-dark mb-5">View students profile</a>
    </div>
    {{/each}}