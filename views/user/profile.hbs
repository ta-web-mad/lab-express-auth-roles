<div class="container">
    <div class="card">
        <div class="row">
            <div class="col-4">
                <img src={{user.profileImg}} alt="{{user.username}}">
            </div>
            <div class="col-8">
                <h1>Bienvenid@, {{user.username}}</h1>
                <hr>
                <p>Tu email es {{user.email}}</p>
            </div>
        </div>
    </div>

</div>