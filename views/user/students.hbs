<div class="container">

    <h1>Nuestros alumnos</h1>
    <hr>

    {{#each users}}
    <p>- {{name}}

        <a href="/usuario/student/{{id}}" class="btn btn-outline-primary">Ver perfil</a>

        {{#if ../isPM}}
        <a href="/usuario/editar" class="btn btn-outline-warning">Editar</a>
        <a href="#" class="btn btn-outline-danger">Eliminar</a>

        {{/if}}


    </p>
    {{/each}}

</div>