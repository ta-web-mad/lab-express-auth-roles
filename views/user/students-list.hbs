<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <table class="table table-striped table-secondary text-center">
                <tbody>
                    <div class="container">
                        {{#each students}}
                        <tr>
                            <td>
                                <p>{{username}}</p>
                            </td>
                            <td>
                                <a class="btn btn-light" href="/students/{{_id}}">Ver detalles</a>
                            </td>
                        </tr>
                        {{/each}}
                </tbody>
                </tr>
            </table>
        </div>
    </div>
</div>