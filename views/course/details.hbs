<div class="row justify-content-center">

    {{!-- NO TERMINADO --}}

    <div class="col-md-8">
        <h1>{{title}}</h1>
        <hr>
        <h2>{{leadTeacher}}</h2>
        <hr>
        {{#each ta}}
        <h2>{{username}}</h2>
        {{/each}}
        <hr>
        <h2>{{description}}</h2>
        <hr>
        {{#each students}}
        <h2>{{username}}</h2>
        {{/each}}
    </div>
</div>