<div class="container">
    <div class="row">
        {{#each students}}
        <div class="col-3">
            <p>{{username}}</p>
            <p>{{email}}</p>
            <p>{{description}}</p>
            <a class="a-button-form" href="/student/{{_id}}">Mostrar estudiante</a>
        </div>
        {{/each}}
    </div>
</div>