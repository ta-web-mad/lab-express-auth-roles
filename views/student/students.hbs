
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
{{#each students}}

<ul>
    <p>{{username}}</p>
    <a href="/students/{{_id}}"><button>View</button></a>
    <a href="/students/{{_id}}"><button>Edit</button></a>

    {{#if isPM}}
    <a href="/students/{{_id}}/edit"><button>Edit</button></a>
    <a href="/students/{{_id}}/delete"><button>Delete</button></a>
    {{/if}}
</ul>

{{/each}}
</div>
</div>
